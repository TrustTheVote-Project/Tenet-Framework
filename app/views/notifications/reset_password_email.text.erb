Hello, <%= @user.first_name %>!

<% unless @user.password_set? %>
You don't have a password to log in with yet.

To set a password, just follow this link:

<%= init_password_url(@user.reset_password_token) %>
<% else %>
You have requested to reset your password.

To choose a new password, just follow this link:

<%= reset_password_url(@user.reset_password_token) %>
<% end %>

Have a great day!
